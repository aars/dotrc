#
# NODE
#

# We're using Node Version Manager
export NVM_DIR="${HOME}/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
[ -r "$NVM_DIR/bash_completion" ] && . "$NVM_DIR/bash_completion"

ZSHCODE_LOADED=1
